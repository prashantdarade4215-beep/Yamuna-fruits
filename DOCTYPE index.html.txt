<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fresh Fruits - Order Online</title>
  <style>
    * {margin: 0; padding: 0; box-sizing: border-box;}
    body {font-family: Arial, sans-serif; background: #f8f8f8; color: #333; scroll-behavior: smooth;}
    header {
      background: linear-gradient(to bottom, rgba(0,0,0,0.4), rgba(0,0,0,0.6)), url('https://images.unsplash.com/photo-1574226516831-e1dff420e43e') no-repeat center/cover;
      color: white; text-align: center; padding: 120px 20px;
    }
    header h1 {font-size: 3em;}
    nav {position: sticky; top: 0; background: #28a745; padding: 10px; text-align: center;}
    nav a {color: white; margin: 0 15px; text-decoration: none; font-weight: bold;}
    nav a:hover {color: #ffd700;}
    section {padding: 40px 20px; max-width: 1100px; margin: auto;}
    h2 {text-align: center; margin-bottom: 20px;}
    .products {
      display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px;
    }
    .product {
      background: white; padding: 10px; border-radius: 10px; box-shadow: 0 4px 8px rgba(0,0,0,0.1);
      text-align: center; transition: transform 0.3s;
    }
    .product:hover {transform: scale(1.05);}
    .product img {width: 100%; height: 200px; object-fit: cover; border-radius: 8px;}
    .price {font-weight: bold; color: #28a745;}
    input[type=number] {width: 60px; padding: 5px; text-align: center;}
    #order-summary {background: white; padding: 20px; border-radius: 10px; box-shadow: 0 4px 8px rgba(0,0,0,0.1); margin-top: 20px;}
    button {
      padding: 10px; background: #28a745; color: white; border: none; border-radius: 5px; cursor: pointer;
    }
    button:hover {background: #218838;}
    footer {background: #28a745; color: white; text-align: center; padding: 15px;}
  </style>
</head>
<body>

  <header>
    <h1>Fresh Fruits</h1>
    <p>Healthy, organic, and straight from the farm to your table</p>
  </header>

  <nav>
    <a href="#products">Order Fruits</a>
    <a href="#contact">Contact</a>
  </nav>

  <section id="products">
    <h2>Order Online</h2>
    <div class="products">
      <div class="product" data-name="Apple" data-price="120">
        <img src="https://images.unsplash.com/photo-1567306226416-28f0efdc88ce" alt="Apple">
        <h3>Apple</h3>
        <p class="price">₹120 / kg</p>
        <input type="number" min="0" value="0"> kg
      </div>
      <div class="product" data-name="Banana" data-price="50">
        <img src="https://images.unsplash.com/photo-1576402187872-9d35f3d9c9a2" alt="Banana">
        <h3>Banana</h3>
        <p class="price">₹50 / dozen</p>
        <input type="number" min="0" value="0"> dozen
      </div>
      <div class="product" data-name="Mango" data-price="150">
        <img src="https://images.unsplash.com/photo-1502741126161-b048400d3610" alt="Mango">
        <h3>Mango</h3>
        <p class="price">₹150 / kg</p>
        <input type="number" min="0" value="0"> kg
      </div>
    </div>

    <div id="order-summary">
      <h3>Order Summary</h3>
      <p id="order-details">No items selected.</p>
      <p><strong>Total: ₹<span id="total">0</span></strong></p>
      <button id="send-order">Send Order via WhatsApp</button>
    </div>
  </section>

  <section id="contact">
    <h2>Contact Us</h2>
    <p style="text-align:center;">📞 Phone: +91 84599 05789<br>✉ Email: info@freshfruits.com</p>
  </section>

  <footer>
    <p>&copy; 2025 Fresh Fruits. All rights reserved.</p>
  </footer>

  <script>
    const products = document.querySelectorAll('.product input');
    const orderDetails = document.getElementById('order-details');
    const totalAmount = document.getElementById('total');
    const sendOrderBtn = document.getElementById('send-order');

    products.forEach(input => {
      input.addEventListener('input', updateOrder);
    });

    function updateOrder() {
      let total = 0;
      let details = "";
      products.forEach(input => {
        let qty = parseInt(input.value);
        if (qty > 0) {
          let name = input.parentElement.getAttribute('data-name');
          let price = parseInt(input.parentElement.getAttribute('data-price'));
          let subtotal = price * qty;
          total += subtotal;
          details += `${name} - ${qty} × ₹${price} = ₹${subtotal}\n`;
        }
      });
      orderDetails.textContent = details || "No items selected.";
      totalAmount.textContent = total;
    }

    sendOrderBtn.addEventListener('click', () => {
      let phoneNumber = "918459905789"; // Your WhatsApp number (with country code, no +)
      let orderText = `Hello, I'd like to order:\n${orderDetails.textContent}\nTotal: ₹${totalAmount.textContent}`;
      window.open(`https://wa.me/${phoneNumber}?text=${encodeURIComponent(orderText)}`, "_blank");
    });
  </script>

</body>
</html>
